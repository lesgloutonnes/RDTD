/**
 * Point d'extension combat (vagues, ennemis, projectiles).
 * La boucle principale reste dans game.js pour limiter les régressions ;
 * extraire progressivement spawnWave / updateEnemies ici si besoin.
 */
export const COMBAT_MODULE_VERSION = 1;
